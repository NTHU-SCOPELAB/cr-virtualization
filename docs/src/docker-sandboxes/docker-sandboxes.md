# Docker Sandboxes
Docker Sandboxes are isolated microVM environments designed to securely execute AI coding agents (such as Claude Code and Copilot CLI). Each sandbox operates with its own dedicated Docker daemon, filesystem, and network stack.

In this assignment, you will work with Docker Sandboxes and compare them with traditional VMs.

[reference](https://docs.docker.com/ai/sandboxes/#get-started)