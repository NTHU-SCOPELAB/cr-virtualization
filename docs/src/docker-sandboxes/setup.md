### Setup Docker sandboxes
Refer to the [link](https://docs.docker.com/ai/sandboxes/install/) to install Docker Sandboxes.

Since we want to compare microVMs with real VMs, please install it directly in your native host environment, not inside a VM.

